echo "HELLO"
gh auth login
gh extension install github/gh-copilot
gh copilot suggest "use fd ton findn file with given string inside it iun the current folder or any subfolder recursively"
  fd -H -e txt -x grep -l 'HISTFILE' {} +
rp
rg
rg HISTFILE
echo $$XDG_DATA_HOME
rg XDG_DATA_HOME
rg "export XDG_DATA_HOME"
echp 
echo $XDG_DATA_HOME
echo $HISTFILE
cat /etc/zshrc
gh copilot
alias
alias | grep copilot
npm install -g @github/copilot
copilot
gh copilot suggest "use fd to find a file containing a given string in current dir and all subdir"
gg
gh
gh copilot suggest --shell-out hello
